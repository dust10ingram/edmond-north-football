'use client';
import {useEffect} from 'react';
import {animate, hover, inView, stagger} from 'motion';
const reduced=()=>window.matchMedia('(prefers-reduced-motion: reduce)').matches;
export default function MotionEnhancements(){useEffect(()=>{if(reduced())return;const cleanups:Array<()=>void>=[];const hero=[...document.querySelectorAll<HTMLElement>('.hero .hero-line,.hero .hero-location,.hero .hero-actions,.hero .hero-stat')];if(hero.length){hero.forEach(item=>{item.style.opacity='0';item.style.transform='translateY(26px)'});const controls=animate(hero,{opacity:1,transform:'translateY(0px)'},{duration:1.3,delay:stagger(.13),ease:[.22,1,.36,1]});cleanups.push(()=>controls.stop())}const reveal=(selector:string,childSelector:string,amount=.16,duration=.48,step=.055)=>{document.querySelectorAll<HTMLElement>(selector).forEach(section=>{const stop=inView(section,()=>{const items=[...section.querySelectorAll<HTMLElement>(childSelector)];if(!items.length)return;items.forEach(item=>{item.style.opacity='0';item.style.transform='translateY(18px)'});animate(items,{opacity:1,transform:'translateY(0px)'},{duration,delay:stagger(step),ease:[.22,1,.36,1]})},{amount});cleanups.push(stop)})};const revealEach=(selector:string,amount=.12,duration=2.1)=>{document.querySelectorAll<HTMLElement>(selector).forEach(item=>{item.style.opacity='0';item.style.transform='translateY(24px)';const stop=inView(item,()=>{animate(item,{opacity:1,transform:'translateY(0px)'},{duration,ease:[.22,1,.36,1]})},{amount});cleanups.push(stop)})};revealEach('.standalone-schedule .schedule-list article,.level-schedule .level-game-list article,.roster-list article');revealEach('.huskies-hero > .eyebrow,.huskies-hero > p:last-child,.coaches > .eyebrow,.coaches > div article,.all-time > .eyebrow,.all-time > p,.all-time > div article,.alumni > .eyebrow,.alumni > p,.alumni-years article,.alumni > small');reveal('.tradition','.eyebrow,blockquote,.roster-cta,.program-levels article',.12,.9,.1);reveal('.weekly-board','.leader-grid article',.12);document.querySelectorAll<HTMLElement>('.upcoming-logo-crop').forEach(mark=>{const stop=inView(mark,()=>{animate(mark,{opacity:[.35,1],transform:['translateX(-10px)','translateX(0px)']},{duration:.75,ease:[.22,1,.36,1]})},{amount:.5});cleanups.push(stop)});document.querySelectorAll<HTMLElement>('.standalone-schedule .schedule-list article').forEach(card=>{const logo=card.querySelector<HTMLElement>('.opponent-mark img');if(!logo)return;const stop=hover(card,()=>{const enter=animate(logo,{translateX:'-47%'},{duration:.22});return()=>{enter.stop();animate(logo,{translateX:'-50%'},{duration:.22})}});cleanups.push(stop)});document.querySelectorAll<HTMLElement>('.kickoff-countdown b[data-unit]').forEach(digit=>{let previous=digit.textContent;const observer=new MutationObserver(()=>{if(digit.textContent===previous)return;previous=digit.textContent;animate(digit,{opacity:[.25,1],transform:['translateY(-7px)','translateY(0px)']},{duration:.25,ease:'easeOut'})});observer.observe(digit,{childList:true,characterData:true,subtree:true});cleanups.push(()=>observer.disconnect())});document.querySelectorAll<HTMLDetailsElement>('.player-profile').forEach(card=>{const detail=card.querySelector<HTMLElement>('.profile-detail');if(!detail)return;const onToggle=()=>{if(card.open)animate(detail,{opacity:[0,1],transform:['translateY(-10px)','translateY(0px)']},{duration:.32,ease:[.22,1,.36,1]})};card.addEventListener('toggle',onToggle);cleanups.push(()=>card.removeEventListener('toggle',onToggle))});document.querySelectorAll<HTMLElement>('.leader-grid article>b').forEach(value=>{const raw=value.childNodes[0]?.textContent?.trim()||'';const target=Number(raw.replace(/,/g,''));if(!Number.isFinite(target))return;const stop=inView(value,()=>{const controls=animate(0,target,{duration:1.05,ease:[.22,1,.36,1],onUpdate:n=>{if(value.childNodes[0])value.childNodes[0].textContent=`${Math.round(n).toLocaleString()} `}});return()=>controls.stop()},{amount:1});cleanups.push(stop)});return()=>cleanups.forEach(stop=>stop())},[]);return null}
