'use client';
import {useState} from 'react';
import {freshman, varsity} from '../roster-data';

export default function Roster(){const [team,setTeam]=useState<'varsity'|'freshman'>('varsity');const players=team==='varsity'?varsity:freshman;const label=team==='varsity'?'Varsity':'Freshman';return <main className="roster-page"><header className="subpage-header"><a href="/">← Back to Edmond North Football</a><img src="/assets/wordmark.png" alt="Edmond North Football"/></header><section className="roster-hero"><p className="eyebrow blue">2026 Edmond North Football</p><h1>Rosters.</h1><p>Meet the Huskies representing the North today and building its future.</p><div><span>{players.length} players</span><a href="/recruiting/">Recruiting center →</a></div></section><section className="roster-list" aria-labelledby="roster-list"><div className="roster-tabs" role="tablist" aria-label="Roster team"><button type="button" role="tab" aria-selected={team==='varsity'} className={team==='varsity'?'active':''} onClick={()=>setTeam('varsity')}>Varsity</button><button type="button" role="tab" aria-selected={team==='freshman'} className={team==='freshman'?'active':''} onClick={()=>setTeam('freshman')}>Freshman</button></div><h2 id="roster-list">{label} · {team==='freshman'?'Class of 2030':'2026 roster'}</h2>{players.map(([number,name,position,year],index)=><article key={`${number}-${name}`}><span className="roster-number">{number}</span><div><strong>{name}</strong><small>{position || 'Position pending'}</small></div><span className="roster-class">{team==='freshman'?'Class of 2030':year ? `Class of ${year}` : 'Class year pending'}</span><span className="roster-index">{String(index+1).padStart(2,'0')}</span></article>)}</section></main>}
